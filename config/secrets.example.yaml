# Copy this file to config/secrets.yaml (gitignored) and fill in.
# NEVER commit config/secrets.yaml. NEVER paste keys into chat.
#
# Key hygiene:
#   - "read"  keys: enable ONLY "Read" / "Read-Only". No trade, NO withdrawal.
#   - IP-whitelist every key to this machine's IP.
#   - "testnet" keys are from the testnet site, separate accounts.
#   - "trade" keys are added ONLY at S9/S10, with "Trade" but NEVER "Withdraw",
#     and a hard notional cap set on the exchange side.

binance:
  read:
    api_key: ""
    api_secret: ""
  testnet:            # https://testnet.binancefuture.com
    api_key: ""
    api_secret: ""
  trade:             # leave empty until S10 human sign-off
    api_key: ""
    api_secret: ""

bybit:
  read:
    api_key: ""
    api_secret: ""
  testnet:           # https://testnet.bybit.com
    api_key: ""
    api_secret: ""
  trade:
    api_key: ""
    api_secret: ""

# optional third-party data
coinglass:
  api_key: ""
